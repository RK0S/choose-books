import { FC } from 'react';
import {BooksWidget} from '../../../widgets/BooksWidget';

const MainPage: FC = () => {
    return (
        <>
            <BooksWidget/>
        </>
    );
};

export default MainPage;